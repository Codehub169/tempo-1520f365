# Environment variables for the hueneu application

# Base URL of the deployed application (used for metadata, etc.)
# NEXT_PUBLIC_BASE_URL='http://localhost:9000'

# Contact Form Configuration (Currently not sending emails as per user request)
# If email sending is added later, variables like these might be needed:
# RESEND_API_KEY='your_resend_api_key'
# MAIL_TO='your_email@example.com'
# MAIL_FROM='noreply@yourdomain.com'
